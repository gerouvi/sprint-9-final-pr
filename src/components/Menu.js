// const Menu = ({ className }) => {
//   return <h1 className={className}>Menu</h1>;
// };

// export default Menu;

const Menu = ({ className }) => {
  return (
    <div className={className}>
      <h1>Menu</h1>
    </div>
  );
};

export default Menu;
